# Dictionary on Click (marketing site)

Static Astro site for the Firefox and Chrome extension.

```bash
npm install
npm run dev
```

Build: `npm run build`. Preview: `npm run preview`. Canonical URL: https://dictionary-on-click.bishwodahal.com
